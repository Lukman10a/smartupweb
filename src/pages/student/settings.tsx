import Header from "@/components/header";
import React from "react";

export default function Settings() {
  return (
    <div>
      <Header title={"Settings"} fromQuery={false} />
    </div>
  );
}
